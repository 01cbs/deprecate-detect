async function checkDeprecatedForNpm(packages) {
  const deprecatedPackages = [];
  // TODO: Add logic to check packages against npm registry for deprecation
  return deprecatedPackages;
}

module.exports = checkDeprecatedForNpm;
